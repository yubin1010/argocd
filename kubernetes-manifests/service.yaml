apiVersion: v1
kind: Service
metadata:
  name: weather-service
  namespace: default
spec:
  selector:
    app: weather-app
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000